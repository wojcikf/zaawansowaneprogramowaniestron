/*import { FormControl } from '@angular/forms';

export class FirstLetterUpper {
  static validate(control: FormControl) {
    return FirstLetterUpper.IsValid(control.value[1]) ? null
      : { FirstLetter: true }
  }
  static IsValid(char) {
    return char == char.toUpperCase();
  }
}
*/
